# Feature Specification: 订单流程重构 + 积分集成 + 论坛改版

**Feature Branch**: `003-order-flow-redesign`
**Created**: 2026-05-04 | **Status**: Clarified

---

## 一、概述

重构 极拓空间 的核心订单流程，从「选购商品→下单」改为「直接提交需求→可选兑换服务→线下交易」模式。同时将积分系统与订单流程深度集成，并重新排版论坛为功能分区模式。

---

## 二、隐藏功能（保留代码）

### 2.1 隐藏「立即选购」入口

| 位置 | 处理方式 |
|---|---|
| 首页 Hero 区 `立即选购` 按钮 | 改为 `立即提交` 跳转 `/submit` |
| 首页 CTA 区 `立即选购` 链接 | 改为 `提交需求` |
| 顶部导航 `立即选购` 链接 | 隐藏，CSS `display:none`（保留代码） |
| 移动端底部导航 `选购` 按钮 | 改为 `提交` 跳转 `/submit` |
| 产品列表页 `/products` | 路由保留但不显示导航入口 |
| 商品详情页 `/products/:id` | 同上 |
| 商品订单相关页面 | 同上 |

> 所有商品相关代码保留，后续可随时恢复。

---

## 三、订单流程重构

### 3.1 新订单状态流转

```
已创建(CREATED)
  → 待接单(PENDING)
    → 进行中(IN_PROGRESS)  ← 管理员必须填写预计交付时间
      → 已结单(COMPLETED)  ← 自动发放奖励积分
任意状态 → 已取消(CANCELLED)  ← 终态
```

**状态说明**：
| 状态 | 说明 | 操作方 |
|---|---|---|
| CREATED | 用户刚提交，等待管理员确认 | 系统自动 |
| PENDING | 管理员已看到，待接单 | 管理员操作 |
| IN_PROGRESS | 已接单，进行中 | 管理员操作（必填交付时间） |
| COMPLETED | 已结单 | 管理员操作（自动发放积分） |
| CANCELLED | 已取消 | 用户或管理员 |

> **变更**：移除 `ACCEPTED` 状态（与 PENDING 合并），移除 `CLOSED`（改为 `CANCELLED`）。

### 3.2 提交需求页面重构（/submit）

**页面布局**：

```
┌─────────────────────────────────────────────┐
│  提交需求                                    │
│                                              │
│  课程名称 *    [____________________]         │
│  需求类型 *    [下拉选择____________]         │
│  年级 *        [下拉选择____________]         │
│  截止日期 *    [日期选择____________]         │
│  联系微信号    [自动带入可修改______]         │
│                                              │
│  ── 积分优惠（可选）──                        │
│  服务套餐/折扣券  [下拉：无 / 我的套餐列表]   │
│                                              │
│  ┌─ 折扣计算预览 ─┐                          │
│  │ 原价参考：200-500元                       │
│  │ 使用折扣券：8折                           │
│  │ 预估折后价：160-400元                     │
│  └────────────────┘                          │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  💡 提交后请添加管理员微信            │    │
│  │  微信号：Jt--04（点击复制）           │    │
│  │  备注订单号，管理员1-2小时内回复      │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  [        确认提交        ]                   │
└─────────────────────────────────────────────┘
```

**下拉选项逻辑**：
1. 查询用户已兑换且未过期的服务套餐和折扣券
2. 服务套餐：显示名称 + 有效期（如「期中作业套餐 · 5月30日前有效」）
3. 折扣券：显示名称 + 折扣比例 + 有效期（如「8折优惠券 · 5月10日前有效」）
4. 选择折扣券后，根据需求类型的参考价格自动计算折后价并展示
5. 选择服务套餐后，标记该订单使用了哪个套餐

### 3.3 创建订单 API 变更

```json
// POST /api/orders
{
  "courseName": "高等数学",
  "orderTypeId": "...",
  "grade": "JUNIOR",
  "deadline": "2026-06-30T00:00:00Z",
  "contactWechat": "wxid_xxx",
  "source": "PC",
  // 新增：可选
  "redeemItemId": "...",     // 兑换的服务套餐/折扣券 ID
  "couponCode": "...",       // 或通过券码选择
}
```

**后端处理**：
1. 创建订单，状态为 `CREATED`
2. 如果传了 `redeemItemId`，验证该兑换项属于当前用户且未过期、未使用
3. 如果是折扣券，计算折后价记录到订单
4. 如果是服务套餐，标记订单关联该套餐
5. 标记兑换项为已使用
6. 返回订单号 + adminWechatId

### 3.4 订单详情页变更

**用户端**：
- 显示状态时间线
- 如果使用了折扣券/服务套餐，显示关联信息
- 进行中状态显示预计交付时间
- 已结单状态显示获得的奖励积分

**管理端**：
- 状态操作按钮：
  - `CREATED` → `PENDING`：「确认接单」
  - `PENDING` → `IN_PROGRESS`：「开始处理」（弹窗填写预计交付时间）
  - `IN_PROGRESS` → `COMPLETED`：「标记结单」（自动计算并发放奖励积分）
  - 任意 → `CANCELLED`：「取消订单」

### 3.5 奖励积分发放

**规则**：
- 订单标记为 `COMPLETED` 时自动发放
- 奖励数量 = 订单金额 × 对应订单类型的积分比例
- 比例在管理后台「需求类型」管理中配置（如：期中作业 5%，毕业设计 10%）
- 如果订单没有报价（quotedPrice），则按需求类型的参考价格中间值计算
- 管理员结单时可手动调整积分数量

**需求类型新增字段**：
```
pointRewardRate: Decimal  // 积分奖励比例，如 0.05 表示 5%
```

---

## 四、积分商城调整

### 4.1 兑换流程变更

**当前**：兑换 → 管理员审核 → 生效
**改为**：兑换 → 满足条件 → 直接生效（无需审核）

**自动生效条件**：
- 用户积分余额 >= 兑换所需积分
- 兑换项有库存（如有库存限制）
- 兑换项在有效期内

### 4.2 兑换项有效期

| 类型 | 有效期 | 说明 |
|---|---|---|
| 服务套餐 | 兑换后 30 天 | 过期自动作废 |
| 折扣券 | 兑换后 7 天 | 过期自动作废 |

### 4.3 兑换项在提交需求时展示

- 查询用户已兑换、未过期、未使用的兑换项
- 按类型分组展示（服务套餐 / 折扣券）
- 折扣券选择后自动计算折后价

---

## 五、论坛改版

### 5.1 功能分区

将论坛从「公告 + 讨论」双 tab 改为多板块模式：

| 板块 | 说明 | 谁能发帖 |
|---|---|---|
| 📢 公告通知 | 平台公告、活动通知 | 仅管理员 |
| 📋 服务说明 | 服务类型介绍、价格说明、流程指引 | 仅管理员 |
| ❓ 常见问题 | FAQ、使用教程 | 仅管理员 |
| 💬 用户交流 | 用户提问、经验分享、评价 | 已验证用户（需审核） |

### 5.2 论坛首页布局

```
┌─────────────────────────────────────────────┐
│  论坛社区                                    │
│                                              │
│  [公告通知] [服务说明] [常见问题] [用户交流]  │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │ 📌 置顶帖标题                        │    │
│  │ 管理员 · 2小时前 · 12评论            │    │
│  └──────────────────────────────────────┘    │
│  ┌──────────────────────────────────────┐    │
│  │ 普通帖标题                           │    │
│  │ 用户昵称 · 1天前 · 5评论             │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  [+ 发帖]  ← 仅「用户交流」板块显示         │
└─────────────────────────────────────────────┘
```

### 5.3 Post 模型变更

```prisma
model Post {
  // 现有字段...
  board: String    @default("exchange")  // 新增：板块标识
  // "announcement" | "service" | "faq" | "exchange"
}
```

---

## 六、数据模型变更

### 6.1 Order 模型

```prisma
model Order {
  // 现有字段...
  status: OrderStatus  // 枚举值变更：CREATED/PENDING/IN_PROGRESS/COMPLETED/CANCELLED
  redeemItemId: String?  // 关联的兑换项
  estimatedDelivery: DateTime?  // 预计交付时间
  rewardPoints: Int?  // 奖励积分数量
}
```

### 6.2 OrderType 模型

```prisma
model OrderType {
  // 现有字段...
  pointRewardRate: Decimal  @default(0.05)  // 积分奖励比例
}
```

### 6.3 RedeemOrder 模型

```prisma
model RedeemOrder {
  // 现有字段...
  usedAt: DateTime?  // 使用时间
  usedOrderId: String?  // 关联的订单 ID
  expiresAt: DateTime  // 过期时间（新增，兑换时计算）
}
```

### 6.4 Post 模型

```prisma
model Post {
  // 现有字段...
  board: String  @default("exchange")  // 板块标识
}
```

---

## 七、API 变更清单

| 接口 | 变更 |
|---|---|
| `POST /api/orders` | 新增 `redeemItemId`/`couponCode` 参数 |
| `PATCH /admin/orders/:id/status` | 适配新状态流转，IN_PROGRESS 必填交付时间，COMPLETED 自动发积分 |
| `GET /api/shop/redeem/my` | 新增过滤：未过期、未使用 |
| `GET /posts` | 新增 `board` 过滤参数 |
| `POST /posts` | 新增 `board` 参数 |
| `PUT /admin/order-types/:id` | 新增 `pointRewardRate` 字段 |

---

## 八、UI 变更清单

| 页面 | 变更 |
|---|---|
| 首页 | 「立即选购」改为「立即提交」，跳转 /submit |
| 导航栏 | 隐藏「立即选购」链接 |
| 提交需求页 | 重写，新增积分优惠选择区 |
| 订单详情页 | 适配新状态，显示交付时间和奖励积分 |
| 管理端订单详情 | 适配新操作按钮和弹窗 |
| 论坛页 | 改为多板块 tab 布局 |
| 积分商城 | 兑换后直接生效，显示有效期倒计时 |
| 管理端需求类型 | 新增积分比例配置字段 |
